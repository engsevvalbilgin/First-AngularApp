
@{
    Layout = "~/Views/Shared/_Layout.cshtml";

}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #b5cef5;
        }

        .container {
            margin: auto;
            margin-top: 100px;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

            .container h2 {
                text-align: center;
            }

        .form-group {
            margin-bottom: 15px;
        }

            .form-group label {
                display: block;
                font-weight: bold;
            }

            .form-group input {
                width: 100%;
                padding: 8px;
                border: 1px solid #ccc;
                border-radius: 4px;
                box-sizing: border-box;
            }

                .form-group input[type="submit"] {
                    background-color: #007bff;
                    color: white;
                    border: none;
                    padding: 10px 20px;
                    cursor: pointer;
                    font-size: 16px;
                    border-radius: 5px;
                }

                    .form-group input[type="submit"]:hover {
                        background-color: #0056b3;
                    }

        .sign-in-button {
            display: block;
            text-align: center;
            margin-top: 10px;
        }

            .sign-in-button button {
                background-color: transparent;
                border: none;
                color: #007bff;
                cursor: pointer;
                font-size: 14px;
            }

                .sign-in-button button:hover {
                    text-decoration: underline;
                }
    </style>
</head>
<body>

    <div class="container">
        <h2>Save New Client</h2>

        @using (Html.BeginForm("AddNewClient", "Client", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            @Html.Hidden("ClientId")
            @Html.Hidden("isActive")
            <div class="form-group">
                <label>Name:</label>
                @Html.TextBox("Name", "", new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label>Surname:</label>
                @Html.TextBox("Surname", "", new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label>Phone:</label>
                @Html.TextBox("PhoneNum", "", new { @class = "form-control" })
            </div>
            <div class="form-group">
                <label>Email:</label>
                @Html.TextBox("email", "", new { @class = "form-control" })
            </div>
            @*<div class="form-group">
                <label>Password:</label>
                @Html.TextBox("password", "", new { @class = "form-control" })
            </div>*@
            <div class="form-group">
                <input type="submit" value="Submit" />
            </div>
        }

        <div class="sign-in-button">
            <button onclick="window.location.href = '/Client/ClientList';">Go Back</button>
        </div>
    </div>

</body>
</html>

@*<html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>AddClient</title>
    </head>
    <body>
        <div>





            @using (Html.BeginForm("AddNewClient", "Client", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {

                @Html.Hidden("ClientId")
                @Html.Hidden("isActive")


                <b>Name: </b> @Html.TextBox("Name", "")
                <b>Surname: </b> @Html.TextBox("Surname", "")
                <b>Phone: </b> @Html.TextBox("PhoneNum", "")
                <b>Email: </b> @Html.TextBox("email", "")
                <b>Password: </b> @Html.TextBox("password", "")
                <input type="submit" value="Submit" />

            }*@




@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}




    </div>
</body>
</html>
